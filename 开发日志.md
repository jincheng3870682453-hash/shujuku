# 开发日志

> 动态数据登记系统 · 从无聊开始的 11 天
>
> 这是我最真实的心路记录，不装。给以后的自己看。

---

## 起因：无聊

暑假，打游戏打腻了，刷视频刷吐了。坐在电脑前发呆：好无聊，干点啥。

看到一个字段写死的登记系统，想加列加不了。要不……我自己写一个？

说干就干。从第一天开始就接着 AI 在写，一个多 G 的项目文件夹，全靠 AI 帮我撑着。

---

## 第 1 天：跑通了

没想什么架构，就是做个能加字段、能填数据、能看列表的东西。敲了一晚上，跑通了。界面丑，但能动，爽。

## 第 2 天：崩了

信心爆棚，疯狂加功能。用户管理、隐私政策、自定义颜色、图表预览、审计日志、审核工作流……全加上。加着加着，代码跑不起来了。折腾到凌晨，一片红。烂尾。

## 第 3 天：两连击

上午优化 UI，优化完太丑了，回归。下午继续挖功能，突然全量报错，全套编码错误，原因不明。没 git，去回收站翻备份。翻出来重来，一个功能加一个跑一次。从此学会了 git。不会用也要被逼着会用，自己不会就让 AI 帮我用。

## 第 4 天：发小红书被喷了

发了个小红书，被喷了。喷子不管，但两条建议在理：SQLite 太单调、UI 太难看。差点又烂尾，但数据库是完好的，稳住了。顺便扔了 15 年的老工具流。

## 第 5 天：换工具流

换到 2026 年的开发环境。之前拧巴的地方，顺了。

## 第 6 天：MySQL 搞出来了

兑现被喷的建议，MySQL 双库跑通了。比 WSL 简单多了。

## 第 7 天：压测

压测跑了一天，修修改改。SQLite 和 MySQL 语法不一样，搞不定兼容性，写了两套应付。几个 API 接口发疯报错，检查没问题，修着修着突然好了。

## 第 8 天：UI 优化

全面优化 UI，AI 帮我保留了很多东西，但也有丢失的。一个一个提需求，"抽卡式"开发，提一次生成一次，不行再提。8 种自定义质感，自定义背景。用户协议和隐私政策完善了。

## 第 9 天：接 AI

突然想到接 AI 数据分析。API Key 暴露了，做加密，输一次就消失。请求被过滤，花好大力气才搞定。30 秒超时不够，AI 要分析整个图表，改长才跑通。Prompt 太宽，AI 输出乱七八糟，收窄之后才能用。

## 第 10 天：打包

折腾 Docker。蓝色小鲸鱼都打不开。WSL 卸不掉，升级卡 16% 崩了。去 GitHub Actions 在线打包，下载要 WSL 验证，先打成压缩包绕过，再拉取到本地。绕了一大圈，好歹成了。

10 天，23 个版本。7 种动态字段，4 级角色 21 项权限，审核+审计日志，AI 多模型分析，4 种部署形态。170 并发 72 小时 0 失败，平均 42ms。

但最难忘的不是这些。

是第 2 天烂尾的挫败，第 3 天翻回收站的狼狈，第 4 天被喷的不服，第 10 天绕一大圈终于跑通的爽。

---

## 第 11 天（08-13）：补窟窿、加个性

昨天发布完心情还飘着。缓了一天，回来接着干。

先说个大窟窿——**HR 删除数据竟然不经过审核**。当时写完审核工作流以为都覆盖了，结果一查，HR 的删除是直接执行的，管理员那边根本看不到。这不行，赶紧改。单条删除和批量删除都改成必须先提交审核，管理员批了才真删。员工那边还是走原来的全局开关。

然后是**主题系统**。之前用户能选颜色、能自定义背景，但配置存在 localStorage 里，换个设备就没了，而且所有账号共享一份。这次改成按账号存后端，登录自动恢复。主题色、玻璃质感、透明度、霓虹灯、背景图都能配，加了个悬浮按钮一键切换。不同账号互不影响，这点很舒服。

接着是** AI 按角色定制**。之前 AI 分析所有账号都是同一套话术、同样范围的数据。这次改成：员工只能分析自己创建的数据，HR 和管理员看全量；话术也按身份来——员工聊个人改进，HR 聊人员管理，管理员聊全局治理。页面顶部加了个身份提示条，一眼看出当前分析的边界。AI 客户端那边也顺带修了一轮：联网搜索只有 DeepSeek 官方端点才开，别的平台自动关并提示；兼容了 OpenAI 新版的流式协议；报错信息改成能看懂的中文。

隐私政策和用户协议也重写了一遍，把 AI 分析、账号权限这些新东西都写进去了。

还有个一直没做的：**AI 分析记录存在了 localStorage**，账号之间全串了。这次建了张表，按用户隔离存后端，换设备不丢，互相看不见。

最后把今天这堆事全写进更新日志，v5.1。桌面上的项目同步到 GitHub。

今天没崩。因为前两天崩怕了，每一步都先跑一下再往下走。挺好。

---

## 做完回头看

11 天，24 个版本。

这几天教会我的事：
1. **git 越早学越好**。第 3 天翻回收站的狼狈，这辈子不想再来一次。
2. **一个功能加一个跑一次**。别像第 2 天那样一次堆一堆，然后整片红。
3. **被喷不全是坏事**。第 4 天的两条建议，最后都成了核心功能。
4. **别一拍脑袋就往下干**，特别是对当下工作流不熟悉的情况下。

最爽的一刻：今天收工，git push 回车，看到绿了。

---

*记录于 2026-08-13 · v5.1*
