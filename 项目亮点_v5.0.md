# 动态数据登记系统 (Dynamic Registry) v5.0 — 项目亮点

## 一句话定位
**企业级动态数据管理平台**：支持 7 种字段自由配置、4 级权限 + 21 项细粒度控制、多模型 AI 对话式分析、4 种部署形态全覆盖（Web / Electron 桌面 / Docker / 单 EXE）。

---

## 十大核心亮点

### 1. 动态字段引擎 — 零代码数据建模
无需改代码，通过可视化界面添加文本、数字、日期、下拉、布尔、长文本、文件上传等 7 种字段类型，数据库自动适配表结构。

### 2. 双数据库引擎 — SQLite + MySQL 无缝切换
基于 ABC 抽象类的 DatabaseAdapter 适配层，一行配置切换 SQLite（零配置桌面场景）和 MySQL（高并发生产场景），业务代码零改动。

### 3. 三级网关安全模型 — 认证 → 权限 → 审核
所有请求依次经过 Session/Token 双模认证、21 项细粒度权限检查、用户级审核开关。非 Boss 操作自动进入待审核队列，审批后自动解析执行，全程审计日志不可篡改。

### 4. AI 多模型对话式数据分析
支持 DeepSeek V4、OpenAI GPT-4o/o3、通义千问 Qwen3、文心一言 ERNIE-4.5 及自定义 OpenAI 兼容端点（Ollama/vLLM）。一键汇总数据库信息调用 AI 生成 Markdown 报告，支持流式输出 + 多轮对话追问。

### 5. 4 种部署形态全覆盖
- **Electron 桌面版** — Windows/macOS/Linux 原生窗口
- **Docker Compose** — MySQL + Flask + Nginx 三服务一键启动
- **PyInstaller 单 EXE** — 便携交付，内嵌浏览器
- **纯 Web 部署** — Nginx + Flask 经典架构

### 6. 企业级审核工作流 — 15+ 种操作类型
覆盖数据新增/编辑/删除、字段增删改、用户管理、数据库重置、备份恢复。Boss 直接执行，HR/Employee 独立审核开关，支持审批评论。

### 7. 170 并发压测 — 0% 失败率
Locust 全场景压力测试：170 并发用户、2100+ 请求、平均响应 42ms、0% 失败率。Waitress 20 线程 + TanStack React Query 缓存，生产级稳定性。

### 8. Linear 暗色设计系统 + 3 种视觉质感
对标 Linear.app「午夜精密仪器」设计语言，提供玻璃/默认/霓虹 3 种质感，5 项自定义配色、背景图上传，CSS 变量驱动实时生效。

### 9. 全链路数据安全
- 密码：PBKDF2-SHA256 加盐哈希
- 数据库：AES-256-GCM 透明加密（可选）
- API Key：仅存内存不落盘
- 审计日志：全量记录不可篡改

### 10. 10 天 23 个版本极速迭代
2026-07-19 到 07-28，从 MVP → beta → rc → 正式版 → 架构重构 → AI 增强 → UX 升级，16 项新增功能，2 项修复，2 项优化。

---

## 技术栈一览

| 层级 | 技术 |
|:---|:---|
| 前端 | React 18 + TypeScript + Vite + Ant Design 5 + TailwindCSS + ECharts |
| 状态管理 | Zustand + TanStack React Query |
| 后端 | Flask 3.x + Waitress |
| 数据库 | SQLite / MySQL 8.0（双引擎适配层） |
| 桌面 | Electron 28 + electron-builder |
| 容器化 | Docker + docker-compose |
| 安全 | AES-256-GCM + PBKDF2-SHA256 |
| 压测 | Locust（170 并发，0% 失败） |

---

## 核心数据
- **45 个 API 端点** · **12 个前端页面** · **4 级角色** · **21 项权限**
- **7 种字段类型** · **15+ 种操作审核** · **5 种 AI 模型**
- **23 个版本** · **10 天开发周期** · **170 并发验证**
